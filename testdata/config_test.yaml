# Template config file.
# Here you can specify the paths to the input and output, and other additional parameters.
# Arguments are specified in a key:value format, do NOT change the key names.


#####################
# Input and output
#####################

# It is recommended to provide absolute paths, i.e. beginning with root (/).
# Else, paths are considered to be relative with the location from which snakemake is called as root.

# Reference protein database.
reference_p: test_data2/ref.fa

# Fastq files containing paired RNA-seq reads.
reads:
  sample_1: #Sample 1. Provide the paired files as a list of 2 items.
    [ test_data2/reads_1_1.fastq, test_data2/reads_1_2.fastq ] # Do not remove the [ and ]. Separate the file names by a comma.
  sample_2: #Sample 2.
    [ test_data2/reads_2_1.fastq, test_data2/reads_2_2.fastq]
  sample_3: #Sample 3.
    [ test_data2/reads_3_1.fastq, test_data2/reads_3_2.fastq]
  sample_4: #Sample 4.
    [ test_data2/reads_4_1.fastq, test_data2/reads_4_2.fastq]
  sample_5: #Sample 5.
    [ test_data2/reads_5_1.fastq, test_data2/reads_5_2.fastq]
  sample_6: #Sample 6.
    [ test_data2/reads_6_1.fastq, test_data2/reads_6_2.fastq]

# Output folder
out_dir: test2_output/


#####################
# Training scoring scheme
#####################

##Uncomment below to disable scoring scheme training by last-train
#training : "no"
